.blog-snippet-container {
  background-color: $darkBackground
  color: $lightText
  padding: $padding

  h2 {
    color: $lightText
  }

  em > span {
    font-size: 14px
  }

  .link-button-container {
    margin-top: $padding
    text-align: right
  }

  .date {
    margin-top: -5px

    span {
      font-size: 14px
    }
  }
}
