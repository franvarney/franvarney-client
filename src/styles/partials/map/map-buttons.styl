.page-map #button-wrap {
  display: inline-block
  position: absolute
  padding: $padding
  right: $padding * 2
  top: $padding
  width: 200px
  z-index: 100

  .link-button {
    margin-bottom: 25px
    position: relative
  }
}
