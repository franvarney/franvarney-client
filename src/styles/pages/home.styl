.page-home {
  #about-me {
    color: $lightText
    padding: $padding

    h2 {
      color: $lightText
    }
  }

  #jobs {
    padding: $padding
  }

  .image {
    display: block
    height: 100%
    max-height: 325px;
    overflow: hidden

    img {
      margin: 0 !important
    }
  }

  #lilac {
    max-height: 364px
  }

  .row:last-child {
    .link-button {
      margin-top: $padding
      text-align: right

      a {
        color: $darkBackground
      }
    }
  }
}
